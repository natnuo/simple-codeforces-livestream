{
  "dependencies": {
    "express": "^4.21.0",
    "express-handlebars": "^8.0.1",
    "js-sha512": "^0.9.0"
  },
  "scripts": {
    "build": "npx tsc",
    "start": "node dist/server.js",
    "test": "npm run build && npm start"
  },
  "devDependencies": {
    "@types/express": "^5.0.0"
  }
}
